pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo "Build is success "
            }
        }

        stage('Testing') {
            steps {
                echo "Testing is success "
            }
        }

        stage('Deploy') {
            steps {
                echo "Deploy is success "
                 echo "Deploy is success "
            }
        }
    }
}
